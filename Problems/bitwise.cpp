#include <iostream>
#include <climits>
using namespace std;

int main() {
    int a = 5;
    int b = 3;

    // cout << "a & b = " << (a & b) << endl; // Bitwise AND
    // cout << "a | b = " << (a | b) << endl; // Bitwise OR
    // cout << "a ^ b = " << (a ^ b) << endl; // Bitwise XOR
    // cout << "~a = " << (~b) << endl;       // Bitwise NOT
    // cout << "a << 31 = " << (a << 31) << endl; // Left shift
    // cout << "b >> 1 = " << (b >> 3) << endl; // Right shift

    cout << LLONG_MAX << endl;

    return 0;
}